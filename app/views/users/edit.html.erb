<h1>ユーザー 編集</h1>
<%= form_with model: @user, local: true do |f| %>
  <p>名前<%= f.text_field :title %></p>
  <p>年齢<%= f.text_field :contents %></p>
  <%= f.submit "送信" %>
  <p><%= link_to "一覧画面へ", users_path %></p>
<% end %>